import React from 'react'
import { Text, View } from 'react-native'

export default function MakesFilter() {
  return (
    <View>
      <Text>
        TODO: Emulate making the call to fetch makes, show a list of makes,
        update store with makes selected, simulate making call to load SRP with
        filtered makes
      </Text>
    </View>
  )
}
